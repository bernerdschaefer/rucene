
if [[ -d "$rvm_path/environments" && -s "$rvm_path/environments/ruby-1.8.7-p302@rucene" ]] ; then
  \. "$rvm_path/environments/ruby-1.8.7-p302@rucene"
else
  rvm --create use  "ruby-1.8.7-p302@rucene"
fi
